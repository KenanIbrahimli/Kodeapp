<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Kodeapp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#FFFFFF</item>
        <item name="colorPrimaryDark">#FFFFFF</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="OwnTextApperance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="OwnToolBar" parent="TextAppearance.Design.Tab">
        <item name="android:background">@drawable/search_bar_</item>
        <item name="colorPrimary">@color/secondary_text</item>
        <item name="colorPrimaryDark">@color/tetriary_text</item>
        <item name="colorAccent">#FFFFFF</item>
    </style>
</resources>